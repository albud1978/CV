# ===========================================
# Computer Vision
# ===========================================
opencv-python>=4.8.0
Pillow>=10.0.0

# ===========================================
# Deep Learning
# ===========================================
# torch>=2.0.0      <-- Installed via Docker base image (pytorch/pytorch)
# torchvision>=0.15.0 <-- Installed via Docker base image
ultralytics>=8.3.0

# SAM 2 (Official Meta Repo)
git+https://github.com/facebookresearch/segment-anything-2.git

# Roboflow / SAM 3 / RF-DETR
roboflow>=1.1.0
inference>=0.9.0
supervision>=0.16.0
rfdetr>=0.0.1         # New! RF-DETR by Roboflow

# Grounding DINO (Open Vocabulary)
# Компилируем из исходников для корректной работы CUDA-слоев
# git+https://github.com/IDEA-Research/GroundingDINO.git

# Molmo 2 (Allen Institute for AI) — VLM для видео/изображений
transformers>=4.57.1
accelerate>=0.34.0
einops>=0.8.0
decord2>=0.6.0
molmo_utils>=0.0.1
bitsandbytes       # Для квантования

# ===========================================
# Data Processing
# ===========================================
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0        # для frames_manifest.parquet

# ===========================================
# Pipeline (СЛОЙ 1 — ingest)
# ===========================================
scenedetect[opencv]>=0.7   # детекция сцен (fallback для подвижных камер)
imagehash>=4.3.0           # perceptual hash для дедупликации кадров

# ===========================================
# Pipeline (СЛОЙ 2 — локальный учитель open-vocabulary)
# ===========================================
# Ставится по потребности: teacher.py перебирает бэкенды и работает с любым из них.
# autodistill>=0.1.0
# autodistill-sam3           # предпочтительный учитель (маски), веса через ROBOFLOW_API_KEY
# autodistill-grounded-sam-2 # запасной вариант (маски)
# autodistill-grounding-dino # запасной вариант (только боксы)

# ===========================================
# Pipeline (СЛОЙ 5 — экспорт на edge и замер задержки)
# ===========================================
# onnx>=1.16.0
# onnxruntime>=1.18.0        # замер задержки на CPU (прокси мобильной производительности)
# ncnn                       # экспорт под Android CPU
# tensorflow>=2.16.0         # нужен ultralytics только для экспорта в TFLite

# ===========================================
# Visualization
# ===========================================
matplotlib>=3.7.0
seaborn>=0.12.0

# ===========================================
# Utilities
# ===========================================
tqdm>=4.65.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Ускорение и устойчивость скачивания больших файлов с HuggingFace
hf_transfer>=0.1.9

# ===========================================
# Jupyter
# ===========================================
jupyter>=1.0.0
ipykernel>=6.0.0
